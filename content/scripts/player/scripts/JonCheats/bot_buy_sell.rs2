

[debugproc,buy_arrow]
mes("Buying Arrows in Varrok");
~bot_open_shop(0_50_53_33_33, lowe);
while(p_finduid(uid) = true & inv_total(inv,coins) > 1000){
    ~buy_stackable_item(archeryshop, 0, 1910, bronze_arrow);
    ~buy_stackable_item(archeryshop, 1, 1475, iron_arrow);
    world_delay(1);
}



[debugproc,buy_thread]
mes("Buying Thread in Al Kharid");
~bot_open_shop(0_51_49_57_58, dommik);
while(p_finduid(uid) = true & inv_total(inv,coins) > 1000){
    ~buy_stackable_item(craftingshop2, 5, 60, thread);
    world_delay(1);
}



[debugproc,buy_feathers]
mes("Buying Feathers in Port Sarim");
~bot_open_shop(0_47_50_5_25, gerrant);
while(p_finduid(uid) = true & inv_total(inv,coins) > 1000){
    ~buy_stackable_item(fishingshop, 6, 960, feather);
    world_delay(1);
}



[debugproc,buy_bait]
mes("Buying Bait in Catherby");
~bot_open_shop(0_44_53_18_51, harry);
while(p_finduid(uid) = true & inv_total(inv,coins) > 1000){
    ~buy_stackable_item(fishingshop2, 4, 1170, fishing_bait);
    world_delay(1);
}



[debugproc,buy_eye]
mes("Buying Eye of Newt in Draynor");
~buy_nonstackable_item_far(0_47_50_7_59, betty, magicshop, 8, 0, eye_of_newt, 0_48_50_20_45, 0_48_50_19_45, 0_47_51_59_12);



[debugproc,buy_vial]
mes("Buying and Filling Vials in Ardougne");
while(inv_total(inv, coins) > 100 & p_finduid(uid) = true){
    ~botrun(0,999,true);
    ~botwalk(0_40_51_54_30);
    
    if(npc_find(coord, aemad, 8, ^vis_off) = true & p_finduid(uid) = true){
        p_opnpc(3);

        while(inv_freespace(inv) > 0 & inv_total(inv, coins) > 100 & p_finduid(uid) = true){
            if(inv_getnum(adventurershop, 6) >= 475){
                    ~buy_item(bronze_arrow, 5);
                }
            world_delay(1);
            if(inv_getnum(adventurershop, 0) > 4){
                    ~buy_item(vial_empty, 5);
                }
            world_delay(1);
        }
        if_close;
    }

    ~botwalk(0_41_51_11_43);
    ~bot_oploc(0_41_51_11_43,loc_1557,1);
    ~botwalk(0_41_51_7_48);

    while(inv_total(inv,vial_empty) > 0 & loc_find(0_41_51_5_47,loc_879) = true & p_finduid(uid) = true){
        ~fill_container_water(vial_empty);
    }

    ~botwalk(0_41_51_11_44);
    ~bot_oploc(0_41_51_11_43,loc_1557,1);
    ~botwalk(0_41_51_31_19);
    ~bot_bank_deposit_all(0_41_51_32_19,1);
}



[debugproc,buy_vial2]
mes("Buying Empty Vials in Tavelry");
while(inv_total(inv, coins) > 100 & p_finduid(uid) = true){
    ~botwalk(0_45_53_56_58);
    ~bot_oploc(0_45_53_55_58,loc_1597,1);   //members gate
    ~bot_oploc(0_45_53_55_58,loc_1558,1);   //members gate
    ~botwalk(0_45_53_21_36);

    if(npc_find(coord, jatix, 8, ^vis_off) = true & p_finduid(uid) = true){
        mes("Found NPC");
        p_opnpc(3);

        while(inv_freespace(inv) > 0 & inv_total(inv, coins) > 100 & p_finduid(uid) = true){
            if(inv_getnum(herbloreshop, 0) > 0){
                    ~buy_item(vial_empty, 5);
                }
                world_delay(1);
        }
        if_close;
    }
    ~botwalk(0_45_53_55_58);
    ~bot_oploc(0_45_53_55_58,loc_1597,1);   //members gate
    ~bot_oploc(0_45_53_55_58,loc_1558,1);   //members gate
    ~botwalk(0_46_52_1_40);
    ~bot_bank_deposit_all(0_46_52_1_39,1);
}




[debugproc,buy_runes]
mes("Buying Runes in Varrok");
~botwalk(0_50_53_53_9);

if(npc_find(coord, aubury, 8, ^vis_off) = true & p_finduid(uid) = true){
    mes("Found NPC");
    p_opnpc(3);

    while(inv_total(inv, coins) > 1000 & p_finduid(uid) = true){
        if(inv_getnum(runeshop, 0) > 1980){
            ~buy_item(firerune, 5);
        }
        if(inv_getnum(runeshop, 1) > 1980){
            ~buy_item(waterrune, 5);
        }
        if(inv_getnum(runeshop, 2) > 1980){
            ~buy_item(airrune, 5);
        }
        if(inv_getnum(runeshop, 3) > 1980){
            ~buy_item(earthrune, 5);
        }
        if(inv_getnum(runeshop, 4) > 975){
            ~buy_item(mindrune, 5);
        }
        if(inv_getnum(runeshop, 6) > 499){
            ~buy_item(chaosrune, 5);
        }
        if(inv_getnum(runeshop, 7) > 499){
            ~buy_item(deathrune, 5);
        }
        world_delay(1);
    }
}



[debugproc,buy_runes2]
mes("Buying Runes in the Mage Arena");
~botwalk(0_39_73_35_43);

if(npc_find(coord, magearena_runeshop, 8, ^vis_off) = true & p_finduid(uid) = true){
    mes("Found NPC");
    p_opnpc(3);

    while(inv_total(inv, coins) > 1000 & p_finduid(uid) = true){
        if(inv_getnum(magearena_runeshop, 0) > 199){
            ~buy_item(firerune, 5);
        }
        if(inv_getnum(magearena_runeshop, 1) > 199){
            ~buy_item(waterrune, 5);
        }
        if(inv_getnum(magearena_runeshop, 2) > 199){
            ~buy_item(airrune, 5);
        }
        if(inv_getnum(magearena_runeshop, 3) > 199){
            ~buy_item(earthrune, 5);
        }
        if(inv_getnum(magearena_runeshop, 4) > 139){
            ~buy_item(mindrune, 5);
        }
        if(inv_getnum(magearena_runeshop, 6) > 249){
            ~buy_item(naturerune, 5);
        }
        if(inv_getnum(magearena_runeshop, 7) > 249){
            ~buy_item(chaosrune, 5);
        }
        if(inv_getnum(magearena_runeshop, 8) > 249){
            ~buy_item(lawrune, 5);
        }
        if(inv_getnum(magearena_runeshop, 9) > 19){
            ~buy_item(cosmicrune, 5);
        }
        if(inv_getnum(magearena_runeshop, 10) > 249){
            ~buy_item(deathrune, 5);
        }
        world_delay(1);
    }
}



[proc,bot_open_shop](coord $shop_location, npc $shop_npc)
~botwalk($shop_location);
if(npc_find(coord, $shop_npc, 8, ^vis_off) = true & p_finduid(uid) = true){
    mes("Found NPC");
    p_opnpc(3);
}



[proc,buy_stackable_item](inv $shop_inv, int $item_slot, int $buy_down_number, obj $buying_item)
while(p_finduid(uid) = true & inv_total(inv, coins) > 1000 & inv_getnum($shop_inv, $item_slot) > $buy_down_number){
    ~buy_item($buying_item, 5);
    world_delay(1);
}




[proc,buy_nonstackable_item](coord $shop_location, npc $shop_npc, inv $shop_inv, int $item_slot, int $buy_down_number, obj $buying_item, coord $bank_walk_coord, coord $bank_coord)
while(inv_total(inv, coins) > 100 & p_finduid(uid) = true){
    ~botwalk($shop_location);
    
    if(npc_find(coord, $shop_npc, 8, ^vis_off) = true & p_finduid(uid) = true){
        mes("Found NPC");
        p_opnpc(3);

        while(inv_freespace(inv) > 0 & inv_total(inv, coins) > 100 & p_finduid(uid) = true){
            if(inv_getnum($shop_inv, $item_slot) > $buy_down_number){
                    ~buy_item($buying_item, 5);
                }
                world_delay(1);
        }
        if_close;
    }
    ~botwalk($bank_walk_coord);
    ~bot_bank_deposit_all($bank_coord,1);
}



[proc,buy_nonstackable_item_far](coord $shop_location, npc $shop_npc, inv $shop_inv, int $item_slot, int $buy_down_number, obj $buying_item, coord $bank_walk_coord, coord $bank_coord, coord $halfway)
while(inv_total(inv, coins) > 100 & p_finduid(uid) = true){
    ~botwalk($halfway);
    ~botwalk($shop_location);
    

    if(npc_find(coord, $shop_npc, 8, ^vis_off) = true & p_finduid(uid) = true){
        mes("Found NPC");
        p_opnpc(3);

        while(inv_freespace(inv) > 0 & inv_total(inv, coins) > 100 & p_finduid(uid) = true){
            if(inv_getnum($shop_inv, $item_slot) > $buy_down_number){
                    ~buy_item($buying_item, 5);
                }
                world_delay(1);
        }
        if_close;
    }
    ~botwalk($halfway);
    ~botwalk($bank_walk_coord);
    ~bot_bank_deposit_all($bank_coord,1);
}



[proc,buy_item](obj $obj, int $amount)
def_int $i = 0;
def_int $current_gp = inv_total(inv, coins);
def_int $shop_stock = calc(inv_total(%shop, $obj));
def_int $space_overflow = inv_itemspace2(inv, $obj, $amount, inv_size(inv));
def_int $total_cost = 0;
// Since pricing and stock will change constantly we will have to iterate here to calc
while($i < $amount & p_finduid(uid) = true) {
    console("looping");
    def_int $added_amt = ~adjusted_item_cost_buying($obj, calc($shop_stock - $i));
    def_int $current_stock = calc($shop_stock - $i);
    if(calc($total_cost + $added_amt) > $current_gp) {
        if ($i > 0) {
            wealth_event(^wealth_shop_buy, oc_debugname($obj), $i, $total_cost);
        }
        mes("You don't have enough coins.");
        return;
    }
    if($current_stock <= 0) {
        if ($i > 0) {
            wealth_event(^wealth_shop_buy, oc_debugname($obj), $i, $total_cost);
        }
        mes("The shop has run out of stock.");
        return;
    }
    // do calc($total_cost + $added_amt) ! $current_gp for the case where the exact gp required for the purchase
    // is used. Not sure if this should be the case in 2004
    if($space_overflow >= calc($amount - $i) & calc($total_cost + $added_amt) ! $current_gp) {
        if ($i > 0) {
            wealth_event(^wealth_shop_buy, oc_debugname($obj), $i, $total_cost);
        }
        mes("You don't have enough inventory space.");
        return;
    }
    $total_cost = calc($total_cost + $added_amt);
    inv_del(inv, coins, $added_amt);
    inv_moveitem_uncert(%shop, inv, $obj, 1);
    $i = calc($i + 1);
}
wealth_event(^wealth_shop_buy, oc_debugname($obj), $i, $total_cost);
console("done looping");



[proc,buy_varrok]
mes("Buying from the shops in Varrok");
while(inv_total(inv,coins) > 100){
    ~bot_open_shop(0_50_53_33_33, lowe);
    while(inv_getnum(archeryshop, 0) > 1910 & inv_getnum(archeryshop, 1) > 1475 & p_finduid(uid) = true & inv_total(inv,coins) > 1000){
        ~buy_stackable_item(archeryshop, 0, 1910, bronze_arrow);
        ~buy_stackable_item(archeryshop, 1, 1475, iron_arrow);
        world_delay(1);
    }
    if_close;


    ~bot_open_shop(0_50_53_53_9, aubury);
    while(inv_getnum(runeshop, 0) > 1980 & inv_getnum(runeshop, 1) > 1980 & inv_getnum(runeshop, 2) > 1980 & inv_getnum(runeshop, 3) > 1980 & inv_getnum(runeshop, 4) > 975 & inv_total(inv, coins) > 1000 & p_finduid(uid) = true){
        ~buy_stackable_item(runeshop, 0, 1980, firerune);
        ~buy_stackable_item(runeshop, 1, 1980, waterrune);
        ~buy_stackable_item(runeshop, 2, 1980, airrune);
        ~buy_stackable_item(runeshop, 3, 1980, earthrune);
        ~buy_stackable_item(runeshop, 4, 975, mindrune);
        ~buy_stackable_item(runeshop, 6, 499, chaosrune);
        ~buy_stackable_item(runeshop, 7, 499, deathrune);

        world_delay(1);
    }
    world_delay(1);
    if_close;
}

